# --- runtime outputs (contain personal file names / signed links) ---
scripts/config.ps1
scripts/seen-videos.txt
scripts/new-videos.m3u

# --- build artifacts ---
app/build/
app/dist/
app/build_log.txt
# spec is regenerated by PyInstaller on each build (may embed absolute
# machine paths) - build.bat recreates it locally, keep it out of git
app/QuarkPlay.spec
__pycache__/
*.pyc

# --- editor / system noise ---
Thumbs.db
Desktop.ini
